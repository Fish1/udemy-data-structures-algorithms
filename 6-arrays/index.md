# Implementing an Array

An implementation of an array in JavaScript can be found in ArrayImplementation.js.

# Strings and Arrays

Treat any strings as arrays. Most functions on a string can be thought of as performation operations on arrays..

# Arrays review

O(N) access speed, O(1) push and pop

Ordered data is great.

Slow to insert/delete data at the begining of an array.

Fixed size makes them hard to grow if we need to add more data.